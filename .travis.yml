language: python
python:
 
#Install dependencies
install:
  - pip install -r requirements.txt

#Run Tests
before_script:
  #- cd ClientAndServerTesting
  #- cd object\ detection

script:
  - pytest ./object\ detection/testing/test.py
  - python3 ./ClientAndServerTesting/newRequestTest.py